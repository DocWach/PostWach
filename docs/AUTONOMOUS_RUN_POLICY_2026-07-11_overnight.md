# Autonomous Overnight Run Policy — 2026-07-11 (PostWach, principal asleep)

**Authored:** Opus 4.8 (claude-opus-4-8[1m]), 2026-07-11, at principal's direction ("run to completion
while I sleep"). This governs what PostWach does autonomously until the principal returns.

## Goal
Run **Round 1** (the stochastic-family structural-record separation theorem) to completion, and
**prep Round 2** (Target B: interfaces as first-class systems + federation/coupling pedigree) up to the
delta-ratification point. Maximize durable progress; make no decision that is the principal's to make.

## Round 1 — stochastic trunk (v2 prompt, RBW-hardened + ratified)
Pipeline: Fable derives → Codex domain-critic → adjudicate.
- **Critic = CLOSED** → finalize: update the morphism-skill currency blocks + the session archive with
  the result; commit the research artifacts (NO push); Round 1 done.
- **Critic = REWORK** → adjudicate the specific blockers, fix the prompt/candidate, re-run Fable.
  **Max 2 REWORK iterations.** If still REWORK after 2, STOP, write a blocker report, do NOT force a
  positive verdict. A REWORK is the process working.

## Round 2 prep — Target B (interfaces / federation / coupling pedigree)
Foundations grounding (running now) → harden prompt → three-leg RBW (Codex/Gemini/Opus) → adjudicate.
- **STOP at the delta-ratification point.** Leave a one-page "ready for your GO" summary.
- Do NOT fire Fable on Target B, and do NOT ratify a delta narrowing, without the principal. Delta
  decisions are the principal's (established pattern: unification→harmonization, stochastic trunk).

## Guardrails
- **No push. No outward-facing actions. No destructive `.claude`/OneDrive operations.**
- Commit research/planning artifacts at each checkpoint (protective, per INCIDENT-001: uncommitted work
  is the exposure). Author = Paul Wach; Co-Authored-By: Claude Opus 4.8; never the ruvnet trailer.
- On genuine ambiguity that needs a principal decision: DOCUMENT it in a short note and continue other
  work; never guess a delta, never stall the whole run on one open question.
- Always keep a background task running or a wakeup scheduled so the loop advances; when all work is
  done or blocked, STOP cleanly with a summary (do not spin).
- Cheap models (Sonnet) for grounding/critic-style reads; Codex/Gemini legs are off the Claude weekly
  budget; Fable for the derivation. Watch the weekly budget (was ~51% at start of night).
- R014 productivity scorecard when the run completes.

## Where to look on wake
- Round 1 result: `research/StochMorph_candidate.md` + `StochMorph_<critic>_2026-07-11.md`.
- Round 2 readiness: `research/Foundations_TargetB_*` + `Fable_Prompt_TargetB_*` + `TargetB_RBW_Adjudication_*`.
- A final `docs/session-archives/` continuation note summarizing the night.
